# Проверка на чистой машине (воспроизводимость)

Чеклист для сдачи блока **«Упаковка и воспроизводимость»**.  
Выполняется на ПК **без** предустановленного проекта (или в новой папке / VM / Docker с нуля).

**Репозиторий:** [github.com/Ekaterina-Kotendzhi/PPC-Audit-Workspace-v.0](https://github.com/Ekaterina-Kotendzhi/PPC-Audit-Workspace-v.0)  
**Версия:** последний commit в ветке `master` на GitHub (после `git clone` проверьте: `git log -1 --oneline`).

---

## Предусловия

| Компонент | Docker-путь | Python-путь |
|-----------|-------------|-------------|
| Git | да | да |
| Docker Desktop | да | — |
| Python 3.12+ | — | да |
| Node.js 18+ | — | да (только вариант B) |
| Ключи API | **не нужны** (демо из `.env.docker.example`) | **не нужны** (пустой `.env`) |
| Интернет | да (первый build: Playwright ~160 MB) | да (npm, pip) |

---

## Вариант A — Docker (рекомендуется для проверяющего)

### Шаги

> **Важно:** не запускайте команды из `C:\WINDOWS\System32` — там нет прав на `git clone` и нет `docker-compose.yml`. Сначала перейдите в папку пользователя.

```powershell
cd $env:USERPROFILE\Documents
git clone https://github.com/Ekaterina-Kotendzhi/PPC-Audit-Workspace-v.0.git PPC-Audit-Workspace-v.0-test
cd PPC-Audit-Workspace-v.0-test
git log -1 --oneline
docker compose up --build
```

1. Дождаться `Application startup complete` (первый build: **5–15 мин**).
2. Открыть http://localhost:8000 — главная без ошибок.
3. **+ Новый аудит** → название клиента, ниша, цель → сохранить.
4. **Данные → Директ** → загрузить тестовый `.xlsx` Мастер-отчёта.
5. (Опционально) **Источники** → отметить материалы **«Будет в AI»**.
6. **Запуск AI** → модалка: согласие → дождаться 100% (**демо** из `.env.docker.example`, ключи не нужны).
7. **Выводы** → подтвердить минимум **1** карточку.
8. **Отчёт** → открыть HTML/PDF preview (новая вкладка).
9. http://localhost:8000/audit-runs — запись `ai_analysis` со status `success`.

### Остановка

```powershell
docker compose down
```

### Ожидаемый результат

| Критерий | OK |
|----------|-----|
| UI открывается за < 30 сек после старта контейнера | ☐ |
| Excel загружается, на Директ видны KPI/риски | ☐ |
| AI завершается без 500 (демо) | ☐ |
| Findings появились на «Выводы» | ☐ |
| PDF/HTML preview открывается | ☐ |
| `audit_runs` содержит input_json и output_json | ☐ |

### Режимы AI в Docker

| Режим | Настройка | Для кого |
|-------|-----------|----------|
| **Демо (сдача)** | `.env.docker.example`: `PPC_FORCE_DEMO_AI=true` | Проверяющий, офлайн-защита |
| **Внешний Claude** | Локальный `.env.docker` (в `.gitignore`): ключи + `PPC_FORCE_DEMO_AI=false` | Разработка |

> В репозитории только **`.env.docker.example`** — без ключей. Файл `.env.docker` не коммитится.

### Известные особенности Docker

- **OCR:** Tesseract в образе; в `.env.docker.example` — `OCR_PROVIDER=disabled`. Для OCR в локальном `.env.docker`: `tesseract_cli`.
- **Chroma/KB:** локальные embeddings в демо — без внешнего API.
- Данные: volume `./data` на хосте.
- Логи: `LOG_LEVEL=DEBUG` в консоли (`docker logs -f ppc_audit_workspace`).

---

## Вариант B — Python venv (Windows)

```powershell
cd $env:USERPROFILE\Documents
git clone https://github.com/Ekaterina-Kotendzhi/PPC-Audit-Workspace-v.0.git PPC-Audit-Workspace-v.0-test
cd PPC-Audit-Workspace-v.0-test
powershell -ExecutionPolicy Bypass -File .\scripts\setup-venv.ps1
copy .env.example .env
# API-ключи НЕ заполнять — останется демо при пустых ключах
cd frontend
npm install
npm run build
cd ..
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level debug --access-log
```

Далее шаги 2–9 из варианта A (URL тот же).

**Время:** ~8–15 мин при первом `setup-venv` + `npm install`.

---

## Автотесты (опционально, ~2–5 мин)

### Локально

```powershell
cd PPC-Audit-Workspace-v.0-test
.\.venv\Scripts\python.exe -m pytest tests/ -q
```

### В Docker

```powershell
docker compose run --rm app python -m pytest tests/ -q
```

Минимум для проверки AI-пайплайна:

```powershell
python -m pytest tests/test_ai_json_parse.py tests/test_ai_analysis_shape.py -q
```

Ожидание: **passed**.

---

## Протокол проверки (заполнить при сдаче)

| Поле | Значение |
|------|----------|
| Дата проверки | |
| ОС | Windows 10/11 / macOS / Linux |
| Способ | Docker / venv |
| Время clone → UI | мин |
| Время clone → PDF preview | мин |
| Версия commit (`git rev-parse --short HEAD`) | |
| Тестовый Excel | да / нет |
| pytest | passed / skipped / failed |
| Скриншот UI / PDF | `docs/screenshots/` (01–04 + `report.pdf`) |
| Замечания | |

### Пример — проверка разработчиком (июнь 2026)

```
Дата: 2026-06-05
ОС: Windows 11
Способ: Docker
commit: git log -1 --oneline после clone (актуальный HEAD на GitHub)
docker compose build: ~5–6 мин (повторный build с кэшем)
docker compose up → HTTP 200 /: ~10 сек
Демо AI: audit_runs, status=success
PDF export: GET /api/audits/1/export/pdf → 200, application/pdf
pytest test_ai_json_parse + test_ai_analysis_shape: passed
Скриншоты: docs/screenshots/ (UI + PDF preview + report.pdf)
Замечания: первый build может упасть на Playwright при timeout — повторить build
```

> Скриншоты для отчёта уже в `docs/screenshots/`. Переснять: `python scripts/capture_report_screenshots.py` (нужен запущенный сервер и Playwright в контейнере).

---

## Устранение проблем

| Симптом | Решение |
|---------|---------|
| `Permission denied` при `git clone` | `cd $env:USERPROFILE\Documents`, повторить clone |
| `no configuration file provided` | `cd` в папку с `docker-compose.yml` |
| Порт 8000 занят | `docker compose down` или сменить порт в `docker-compose.yml` |
| Build падает на Playwright | Стабильный интернет; повторить `docker compose build` |
| Пустой UI (нет стилей/JS) | Пересобрать образ — frontend stage собирает `app.js` |
| AI failed (демо) | `.env.docker.example`: `PPC_FORCE_DEMO_AI=true` |
| AI failed (Claude) | Проверить ключ ProxyAPI; логи: `docker logs ppc_audit_workspace`; обновить репозиторий |
| `Validation error` charts/schemes | Обновить репозиторий (автодополнение схемы ответа AI) |
| PDF export 500 | Обновить репозиторий; пересобрать образ `docker compose up --build` |
| PDF пустой | Подтвердить вывод на «Выводы», refresh summary на «Отчёт» |
| OCR «Tesseract не найден» | В Docker: образ уже с Tesseract; включить `OCR_PROVIDER=tesseract_cli` |

---

## Связанные документы

| | Файл |
|---|------|
| Быстрый старт, API | [README.md](README.md) |
| Отчёт | [ОТЧЕТ.md](ОТЧЕТ.md) |
| Защита | [ЗАЩИТА.md](ЗАЩИТА.md) |
| Скриншоты UI / PDF | [docs/screenshots/](docs/screenshots/) |
| Чеклист сдачи | [ЧЕКЛИСТ-СДАЧИ.md](ЧЕКЛИСТ-СДАЧИ.md) |
| Видео | [ССЫЛКИ-ВИДЕО.md](ССЫЛКИ-ВИДЕО.md) |

---

*После успешной проверки отметьте чекбоксы и сохраните протокол в отчёт или приложение к сдаче.*
